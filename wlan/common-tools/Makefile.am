# Makefile.am - Automake script for qcacld-tools

ACLOCAL_AMFLAGS = -I m4

dirs :

dirs = cnssdiag
dirs += dppdaemon
dirs += ctrl_app_dut
if CONFIG_WLAN_QCLINUX
dirs += vendorcmdtool
endif

SUBDIRS = $(dirs)
