/*===================================================================================
  Copyright (c) 2020, 2023 Qualcomm Technologies, Inc.
  All Rights Reserved.
  Confidential and Proprietary - Qualcomm Technologies, Inc.
  ===================================================================================*/
/*===================================================================================
FILE:  ITUISessionCallBack.idl

DESCRIPTION:  This file contains interface to the TUISession CallBack, which is
normally called by TUICoreService to notify app of fatal errors and events.

=====================================================================================*/
#pragma once
// AUTOGENERATED FILE: DO NOT EDIT

#include <stdint.h>
#include "object.h"
#include "ITUISessionCallBack.h"

#define TUISessionCallBack_DEFINE_INVOKE(func, prefix, type) \
  int32_t func(ObjectCxt h, ObjectOp op, ObjectArg *a, ObjectCounts k) \
  { \
    type me = (type) h; \
    switch (ObjectOp_methodID(op)) { \
      case Object_OP_release: { \
        if (k != ObjectCounts_pack(0, 0, 0, 0)) { \
          break; \
        } \
        return prefix##release(me); \
      } \
      case Object_OP_retain: { \
        if (k != ObjectCounts_pack(0, 0, 0, 0)) { \
          break; \
        } \
        return prefix##retain(me); \
      } \
      case TUISessionCallBack_OP_OnEvent: { \
        if (k != ObjectCounts_pack(1, 0, 0, 0) || \
          a[0].b.size != 8) { \
          break; \
        } \
        const uint64_t *event_ptr = (const uint64_t*)a[0].b.ptr; \
        return prefix##OnEvent(me, *event_ptr); \
      } \
      case TUISessionCallBack_OP_OnError: { \
        if (k != ObjectCounts_pack(0, 0, 0, 0)) { \
          break; \
        } \
        return prefix##OnError(me); \
      } \
    } \
    return Object_ERROR_INVALID; \
  }


