/** @file ITProcessLoader.idl */
// Copyright (c) 2022 Qualcomm Technologies, Inc.
// All Rights Reserved.
// Confidential and Proprietary - Qualcomm Technologies, Inc.
#pragma once
// AUTOGENERATED FILE: DO NOT EDIT

#include <stdint.h>
#include "object.h"
#include "ITProcessController.h"
#include "ITProcessLoader.h"

#define ITProcessLoader_DEFINE_INVOKE(func, prefix, type) \
  int32_t func(ObjectCxt h, ObjectOp op, ObjectArg *a, ObjectCounts k) \
  { \
    type me = (type) h; \
    switch (ObjectOp_methodID(op)) { \
      case Object_OP_release: { \
        if (k != ObjectCounts_pack(0, 0, 0, 0)) { \
          break; \
        } \
        return prefix##release(me); \
      } \
      case Object_OP_retain: { \
        if (k != ObjectCounts_pack(0, 0, 0, 0)) { \
          break; \
        } \
        return prefix##retain(me); \
      } \
      case ITProcessLoader_OP_loadFromBuffer: { \
        if (k != ObjectCounts_pack(0, 0, 1, 1)) { \
          break; \
        } \
        return prefix##loadFromBuffer(me, a[0].o, &a[1].o); \
      } \
    } \
    return Object_ERROR_INVALID; \
  }


