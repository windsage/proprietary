#pragma once
/** @file ITAccessPermissions.idl */

// Copyright (c) 2023 Qualcomm Technologies, Inc.

// All Rights Reserved.

// Confidential and Proprietary - Qualcomm Technologies, Inc.

// include "ICredentials.idl"

/**
  ITAccessPermissions provides definition of data structures for confinement.
*/

// ITAccessPermissions are optional and only required

//   when memory must be confined to specific users only.

/**
 * @addtogroup ITAccessPermissions
 * @{
 */

/** @cond */

// AUTOGENERATED FILE: DO NOT EDIT

#include <cstdint>
#include "object.h"
#include "proxy_base.hpp"

struct ITAccessPermissions_uidBasedPerms {
    uint64_t uid;
    uint64_t perms;
};

struct ITAccessPermissions_didBasedPerms {
    uint64_t did;
    uint64_t perms;
};

static const uint64_t ITAccessPermissions_keepSelfAccess = UINT64_C(0);

static const uint64_t ITAccessPermissions_removeSelfAccess = UINT64_C(1);

static const uint64_t ITAccessPermissions_smmuProxyControlled = UINT64_C(2);

static const uint64_t ITAccessPermissions_mixedControlled = UINT64_C(4);

struct ITAccessPermissions_rules {
    ITAccessPermissions_uidBasedPerms uidPermsList[5];
    ITAccessPermissions_didBasedPerms didPermsList[5];
    uint64_t specialRules;
};


